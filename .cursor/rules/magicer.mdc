---
description: Magicer 项目专属规则
alwaysApply: true
---

# Magicer 项目规则

## Cursor Good 规则（强制）

在 **Cursor** 中为本仓库工作时，**必须**加载并遵循全局 **`~/.cursor/rules/main.mdc`** 中关于 **CursorGood** 的全部要求（含：任务阶段结束、等待用户下一步、每轮交互闭环前须调用 MCP **`user-cursor-good` → `CursorGood`** 等）。

- 若当前环境已启用 **`user-cursor-good`** MCP：**禁止**在未调用 **`CursorGood`** 的情况下结束对话回合（空返回时按 `main.mdc` 再次轮询）。
- 本条款与 `main.mdc` 冲突时，以 **`main.mdc`** 为准；本条款仅强调在本仓库内**不得省略** Cursor Good 流程。

## README 同步规则（强制）

**每次完成功能修改、发布新版本后，必须同步更新 `README.md`**，包括但不限于：

- `功能特性` 章节：新增/修改/删除的功能点
- `变更日志` 章节：在最顶部追加当前版本的变更说明
- `目录结构` 章节：如有新增或删除文件
- badge 中的版本号

**禁止**在 README 未更新的情况下完成 release。

## DMG 管理规则

- 项目根目录和 `dist/` 目录各只保留**最新一个** `.dmg` 文件
- **每次打包（含手动构建）必须在桌面放一份最新 DMG**，旧桌面 DMG 同时清理
- `release.sh` 脚本会自动完成上述全部清理 + 复制，手动构建时需同步执行：`rm -f ~/Desktop/Magicer-*.dmg && cp *.dmg ~/Desktop/`

## 版本管理策略

### 版本规则（语义化版本）

| 变更类型 | 操作 | 示例 |
|---|---|---|
| Bug 修复 / 小优化 | patch +1 | `1.0.0` → `1.0.1` |
| 新功能（向下兼容） | minor +1，patch 归零 | `1.0.1` → `1.1.0` |
| 破坏性变更 | major +1，minor/patch 归零 | `1.1.0` → `2.0.0` |

### 每次代码修改完成后（自动执行完整发布）

```bash
# 1. 升版本号
echo "x.y.z" > VERSION
# 2. 更新 README.md badge + 变更日志
# 3. 执行完整发布（构建 DMG、复制到桌面、git commit/push/tag、GitHub Release）
./release.sh
```

**每次代码修改后必须执行 `./release.sh`**，生成 DMG 并复制到桌面。

## 技术栈约束

- SwiftUI + AppKit 混合（UI 用 SwiftUI，系统集成用 AppKit）
- 数据持久化：`UserDefaults`（Codable 序列化）
- 无第三方依赖（纯 Swift Package）
- 最低支持 macOS 13.0

## 文件结构规范

- 新功能模块放在 `Sources/` 对应子目录下
- Controller/Manager 类用 `class`（需要引用语义），View 用 `struct`
- 共享状态用 `ObservableObject` + `@Published`
