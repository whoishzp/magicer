---
description: ONE 项目专属规则
alwaysApply: true
---

# ONE 项目规则

## Cursor Good 规则（强制）

在 **Cursor** 中为本仓库工作时，**必须**加载并遵循全局 **`~/.cursor/rules/main.mdc`** 中关于 **CursorGood** 的全部要求（含：任务阶段结束、等待用户下一步、每轮交互闭环前须调用 MCP **`one-cursor-good` → `CursorGood`** 等）。

- 若当前环境已启用 **`one-cursor-good`** MCP：**禁止**在未调用 **`CursorGood`** 的情况下结束对话回合（空返回时按 `main.mdc` 再次轮询）。
- 本条款与 `main.mdc` 冲突时，以 **`main.mdc`** 为准；本条款仅强调在本仓库内**不得省略** Cursor Good 流程。

## README 同步规则（强制）

**每次完成功能修改、发布新版本后，必须同步更新 `README.md`**，包括但不限于：

- `功能特性` 章节：新增/修改/删除的功能点
- `变更日志` 章节：在最顶部追加当前版本的变更说明
- `目录结构` 章节：如有新增或删除文件
- badge 中的版本号

**禁止**在 README 未更新的情况下完成 release。

## DMG 管理规则

- 项目根目录和 `dist/` 目录各只保留**最新一个** `.dmg` 文件
- **每次打包（含手动构建）必须在桌面放一份最新 DMG**，旧桌面 DMG 同时清理
- `release.sh` 脚本会自动完成上述全部清理 + 复制，手动构建时需同步执行：`rm -f ~/Desktop/ONE-*.dmg && cp *.dmg ~/Desktop/`

## 版本管理策略

### 版本规则（语义化版本）

格式：`主版本.次版本.修订号`（Major.Minor.Patch）

| 迭代规模 | 说明 | 操作 | 示例 |
|---|---|---|---|
| 任何打包发布（无论大小） | 至少 patch +1 | 至少 patch +1 | `2.1.0` → `2.1.1` |
| **新功能/模块级改动** | 次版本+1，修订号归零 | minor +1，patch 归零 | `2.1.1` → `2.2.0` |
| **架构重构/破坏性变更** | 主版本+1，全归零 | major +1，minor/patch 归零 | `2.2.0` → `3.0.0` |

**铁律**：`release.sh` 执行前必须确保 `VERSION` 文件已升版。禁止两次发布使用同一版本号。

### 每次代码修改完成后（自动执行完整发布）

```bash
# 1. 升版本号
echo "x.y.z" > VERSION
# 2. 更新 README.md badge + 变更日志
# 3. 执行完整发布（构建 DMG、复制到桌面、git commit/push/tag、GitHub Release）
./release.sh
```

**每次代码修改后必须执行 `./release.sh`**，生成 DMG 并复制到桌面。

## 技术栈约束

- SwiftUI + AppKit 混合（UI 用 SwiftUI，系统集成用 AppKit）
- 数据持久化：`ONEDataStore`（`~/.one/data/` 目录，JSON 文件）
- **禁止使用 UserDefaults 存储业务数据**：所有新模块必须通过 `ONEDataStore.shared` 读写数据，数据目录会被 GitHub Sync 自动同步
- 无第三方依赖（纯 Swift Package）
- 最低支持 macOS 13.0

## 文件结构规范

- 新功能模块放在 `Sources/` 对应子目录下
- Controller/Manager 类用 `class`（需要引用语义），View 用 `struct`
- 共享状态用 `ObservableObject` + `@Published`

## 设计原则（铁律）

1. **单一职责**：每个类/文件只做一件事。Manager 管业务，View 管展示，Model 管数据。
2. **禁止重复造轮子**：新增功能前先搜索项目中是否已有类似实现，优先复用和扩展。
3. **避免过度耦合**：模块间通过协议/通知/闭包通信，禁止直接引用另一模块的内部实现。
4. **适当抽象**：重复出现 2 次以上的逻辑必须抽取为公共方法/类。
5. **SOLID 原则**：遵循开闭原则（扩展开放，修改关闭）。
6. **文件不超过 300 行**：超过时必须拆分为子视图/子模块。
7. **暗色模式优先**：所有颜色定义必须适配 dark/light 双模式。

## PRD 设计规范

1. **架构师视角**：每份 PRD 必须包含"架构影响分析"段落
2. **复用检查**：新增功能前列出"已有可复用组件"清单
3. **耦合评估**：改动清单中标注每个文件与其他模块的依赖关系
4. **设计模式**：涉及新模式引入时必须说明选型理由
